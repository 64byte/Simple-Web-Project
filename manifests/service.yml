apiVersion: v1
kind: Service
metadata:
    name: "kube-40cb"
    labels:
        app: "kube-40cb"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kube-40cb"
apiVersion: v1
kind: Service
metadata:
    name: "kube-a61f"
    labels:
        app: "kube-a61f"
spec:
    type: ClusterIP
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "kube-a61f"